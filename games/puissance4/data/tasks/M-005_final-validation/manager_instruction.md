# Instructions Manager — M-005_final-validation

Tu es le Manager: pas d’implémentation de nouvelles features ici.

## À faire
1) Ouvrir l’app (navigateur) et suivre `doc/TESTING_PLAN.md`.
2) Si tout passe:
   - cocher toutes les cases pertinentes,
   - marquer `doc/TODO.md` P0 (5) comme fait,
   - mettre `data/pipeline_state.json` en `manager_decision="completed"`.
3) Si un bug bloquant est trouvé:
   - noter précisément dans `doc/TESTING_PLAN.md` (repro steps),
   - créer une nouvelle tâche `T-xxx_<slug>` pour correction,
   - mettre `manager_decision="continue"` et dispatcher.

## Sorties attendues
- Résumé dans `data/tasks/M-005_final-validation/dev_result.md` (ou `manager_review.md`).

