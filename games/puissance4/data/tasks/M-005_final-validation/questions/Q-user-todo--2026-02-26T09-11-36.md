# User updated doc/TODO.md (resync required)

The user modified doc/TODO.md via the UI TODO editor and requested a resync before continuing.

Your job now:
1) Re-read doc/TODO.md and ensure tasks + ordering + DoD are consistent.
2) If needed, update/create task folders under data/tasks/ (task.md + manager_instruction.md).
3) Update data/pipeline_state.json so the pipeline can continue safely:
   - If the CURRENT task already has developer outputs (e.g. M-005_final-validation/dev_result.json or M-005_final-validation/dev_result.md exists): set developer_status=ready_for_review (do NOT re-dispatch).
   - Else: set developer_status=ongoing (or blocked if a clarification is required).
   - Ensure current_task_id + assigned_developer are correct.
4) If the TODO changes scope, update doc/SPEC.md and/or doc/DECISIONS.md accordingly.

After resync: continue the pipeline normally (dispatch the next developer task).
